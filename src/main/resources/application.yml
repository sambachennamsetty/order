server:
  port: 9094

spring:
  profiles:
    active: dev
  application:
    name: ORDER-SERVICE
  data:
    mongodb:
      uri: mongodb://localhost:27017
      database: orderdb
      repositories:
        enabled: true
